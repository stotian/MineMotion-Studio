# Changelog

### Minecraft Creation Suite — real functional Phases 815–1014

- Added 200 distinct artist-facing functions across eleven large production areas: bounded worlds, mod assets, building, modeling, rigging, collision, VFX, post, performance, packaging and persistence.
- Added seed/version/loader configuration for Vanilla, Fabric, Forge, NeoForge and Quilt projects, deterministic bounded staging terrain, exact-save guardrails and camera-aware chunk LOD streaming.
- Added safe mod manifests and imported resource-pack/OBJ/Blockbench binding without executing untrusted mod JAR code.
- Added non-destructive world edits, twelve procedural structures, 3D brushes, mirror/duplicate layers and reusable relative blueprints.
- Added voxel primitives/templates, transforms, cleanup, OBJ compilation, renderer synchronization and world-selection extraction.
- Added ready Steve/Alex and Vanilla mob rigs, crowd formations, shared animation operations and editable auto-rigging of voxel models with reload-safe custom geometry.
- Added independently toggleable world/entity collisions, helpers, ground snapping, overlap resolution and sampled timeline collision-avoidance bakes.
- Added twenty Minecraft VFX recipes, sixteen post finishes, ordered non-destructive post stacks, target-device optimization and complete Creation Suite package export.
- Added a six-tab professional Production UI for World, Build, Model, Rig, Collision and Finish workflows.
- Expanded `verify:director` to **414/414 functional phases and 787 assertions**; added `docs/MINECRAFT_CREATION_SUITE_PHASES_815_1014.md`.
- Kept version `0.8.2` and `V1_BLOCKED`: dependency-backed full builds/tests, reviewed WebGL output, native packages, codecs and measured hardware evidence remain unavailable.

### Studio Pro — real functional Phases 716–814

- Added ten physical cinema-camera profiles, six professional camera moves, automatic framing/focus/tracking and horizon stabilization.
- Added persistent take ratings, favorites, rejection/restore, approval, notes, normalized tags, revisions, comparison and naming tools, with reload-safe production sanitization and an in-workspace comparison list.
- Added eight shot-batch operations, eight generated studio-lighting rigs, eight animation-polish actions and continuity analysis/repair for axis crossings, lens consistency and dialogue eyelines.
- Connected visible project light entities to real bounded Three.js point lights in the shared renderer, including optional shadows and editor helpers.
- Added a Studio render pipeline with preview/final/compositing profiles, selected/approved/active scopes, duplicate prevention/removal, workload estimates, sorting, active-shot priority, retry/cancel, stale-revision cleanup, shot synchronization and JSON manifests.
- Added six-department Studio quality control for camera, takes, lighting, continuity, audio and render readiness, with worst-shot focus, automatic repeatable repairs, ready-state promotion and Markdown reports.
- Added reload-safe non-destructive shot creative variants that snapshot and restore camera, lighting, scene lights, post-processing and render passes, with comparison, rating, recapture, best-choice application and JSON manifests.
- Expanded `verify:director` to **214/214 functional phases and 603 assertions**; added `docs/DIRECTOR_STUDIO_PRO_PHASES_716_814.md`.
- Kept version `0.8.2` and `V1_BLOCKED`: dependency-backed UI/build tests, reviewed WebGL frames, native packages, codecs and measured film-quality evidence remain unavailable.


## Unreleased

### Minecraft Director — real functional Phases 601–715

- Added 115 distinct artist-facing Minecraft filmmaking functions instead of roadmap-only phase descriptors.
- Added 14 camera-shot recipes, three generated sequence families, runtime Camera Cuts playback, storyboard synchronization and sequence analysis.
- Added shot split, independent take duplication, animated move/retime/reverse/smoothing/handheld operations, gap closing and ripple delete.
- Added nine cinematic looks, automatic casting, one-click film starters, eight actor actions, two paired choreography blocks, six group formations and 11 Minecraft-native actions.
- Added script-to-scene dialogue timing, recording placeholders, lip-sync cues, performance blocking and automatic camera coverage.
- Added eight narrative beats, eight combined VFX/action events, five transitions, six environment arcs and seven layered sound-design beats.
- Added fail-closed film preflight/repair and five production document exports.
- Added a strict `verify:director` gate: Phases 601–715 must be contiguous, unique, point to existing source owners and execute all 115 unique acceptance IDs. Current isolated result: 447 assertions pass.
- Added an integrated Minecraft Director section in the Production workspace with EN/FR parity.

### Ultra roadmap and source foundations — Phases 84–600

- Added a canonical 517-phase instruction plan extending Ultra from Phase 84 through Phase 600 across 31 production programs.
- Added dedicated deterministic engines and 52 focused acceptance tests for offline rendering, modeling, animation editing, editorial finishing and production workflow Phases 84–135.
- Added versioned capability records and 31 specialized program engines for Phases 136–600, with deterministic execution plans, budgets, fallbacks, evidence contracts and unique test IDs.
- Expanded the Ultra workspace to 35 selectable domains with capability search while preserving the Phase 36–83 legacy storage model.
- Expanded `npm run verify:ultra` to 565 phase tests, 52 dedicated foundation tests, 1,263 top-level assertions and 7,986 internal phase-contract assertions, plus a reproducible roadmap drift gate.
- Kept later phases explicitly at program-engine/source-foundation maturity until final artist workflows, visual, native and hardware evidence exist.

### Ultra production foundations — Phases 36–83

- Added one versioned `ultra` project subdocument covering performance, directing, Minecraft entities, world simulation, rendering, VFX and color.
- Added five deterministic bounded domain engines and phase-specific fail-closed validation for all 48 phases.
- Added schema-10 migration/round-trip coverage, a Production workspace inspector, English/French UI, and CI acceptance through `npm run verify:ultra`.
- Added cinematic material, light-linking, volumetric, sky/cloud, VFX graph, Minecraft effect, compositing graph and color-scope foundations through Phase 83.
- Kept artistic, visual, neural-mocap, high-scale performance and native-release claims blocked until real production evidence exists.

### v1 release engineering

- Frozen project schema 10, package schema 1, settings schema 2, template schema 1, and extension API 1.0 as the target v1 public contracts.
- Added a fail-closed definition-of-done gate, security/legal verification, third-party notices, guarded release workflow, and explicit blocked-evidence registry.
- The product remains unreleased while clean install/build/audit, native installers, manual visual QA, remote CI, signing and publication gates are incomplete.

### Deterministic simulation

- Added bounded seeded debris, collision, cloth, shockwave, crowd-path, camera-noise and wind solvers.
- Added cancellable editable bakes, stale-cache invalidation and production controls.

### Compositing and passes

- Unified preview and offline post-processing plans.
- Added aligned depth, normals and object-ID data passes beside beauty and layer outputs.
- Added per-shot post overrides, pass metadata and bounded render-target pooling.

### Audio synchronization

- Added frame-accurate clip offsets, fades, gain, pan, mute and track roles.
- Added bounded waveform analysis, sync/lip markers, cancellable WAV mixdown and stem handoff metadata.
- Added a focused audio synchronization workspace.

### Asset pipeline

- Added a versioned portable asset catalog with integrity, storage policies, references, favorites and recents.
- Added bounded import inspection, asynchronous thumbnails, duplicate detection, relink and dependency reports.
- Added safe unused-asset preview and cleanup with project-aware reference protection.

### Added
- Premium persistent production workspaces with resizable/collapsible panels.
- Searchable diagnostic outliner, schema-driven transform inspector, timeline zoom/density, and real light theme.

### Changed
- Consolidated the top toolbar and removed the duplicate `Ctrl+S` command.
- Migrated application settings to schema 2 with bounded workspace recovery.


### Earlier local completion work

- Completed Phases 22-25 locally: professional shots/takes/storyboards, seven real render passes and deterministic external-editor handoff.
- Added SDK 1.0 with data-only content packs, capability-gated worker plugins, trust/safe mode, local management, validation tooling and four examples.
- Replaced new `.minemotion` saves with bounded stored ZIP packages while preserving historical JSON and legacy project migration.
- Added bounded autosave history, accessible recovery, redacted diagnostics, accessibility display modes, restricted native open/save, file associations and cancellable FFmpeg jobs.
- Added an evidence-ranked frontier matrix and promoted only deterministic procedural Minecraft crowds behind a feature flag with an 80-actor cap and resource estimates.
- Completed the production-bounded Minecraft world/environment pipeline with
  modern/legacy packed block states, negative Y, custom dimensions, corrupt
  chunk isolation, saved import profiles, changed-chunk reimport, and read-only
  staging overrides.
- Added per-face and supported animated resource-pack textures, explicit
  transparent/emissive/water/leaves/biome-tint fallbacks, and missing-face reports.
- Added animation-ready sun/moon/weather/wind environments plus Nether and End
  moods shared by preview/export frame state.
- Added versioned fingerprinted portable world-cache assets, package size
  warnings, reference-only saves, and degraded recovery for missing/corrupt caches.
- Split characterized Timeline/NLA presentation from `TimelinePanel` while
  preserving canonical project, command, selection, playback, and history owners.
- Added localized non-destructive Draft-budget diagnostics and prioritized
  optimization recommendations without automatic quality changes.
- Added deterministic small, medium, large-world, VFX-fight, and storm benchmark
  projects with expected complexity and budget classifications.
- Added an explicit WebGL2/WebGL production backend policy that records WebGPU
  as experimental evidence but never selects it silently.
- Added shared measured bundle/architecture/VFX baselines and post-build CI
  regression ceilings.
- Migrated projects to schema 10 with native deterministic VFX persistence.
- Added the stable built-in VFX library, safe VFX authoring/packages, and
  English/French localization.
- Consolidated global rig animation authority and added deterministic Steve/Alex
  two-bone IK controls, live preview, and timeline baking.
- Added deterministic imported/preset terrain sampling and atomic grounded
  left/right foot-lock range baking.
- Added bounded head, camera, and object look-at preview with animated target
  selection and atomic bake to existing rotation tracks.
- Added derived character root, left/right hand, and camera motion paths with
  timeline ranges, key points, statistics, and export-safe viewport overlays.
- Evolved the existing NLA tracks into six bounded animation layers with scoped
  override/additive blending, mute/weight controls, shared production sampling,
  and VFX synchronization metadata.
- Added ten bounded deterministic procedural rig generators for idle, locomotion,
  jump/landing, recoil/hit, sword swing, and turn motion with editable outputs
  and localized Rig Studio controls.
- Added selection-scoped redundant-key cleanup, bounded noise reduction,
  smoothing, duration-bounded loop, easing-aware reverse, and rig/transform
  mirror tools to the existing animation command bar.
- Added a session-only rig pose clipboard with compatible-bone paste, bounded
  blending, no-op-aware mirror/reset, and localized Rig Studio controls.
- Added validated rig attachment controls, parent-bone animation coverage, and
  real imported OBJ rendering through the shared preview/export scene path.
- Improved Blockbench import with bounded current outliners, nested pivots and
  rotations, deterministic static OBJ output, preserved texture/clip metadata,
  reconciled persistence, and localized capability reports.
- Added conservative automatic and preset-scoped manual Blockbench bone
  mapping, plus deterministic numeric rotation clip conversion onto the
  existing editable animation timeline.
- Added optional bounded pixel-expression overlays for blink, anger, sadness,
  confidence, surprise, and fear through the shared character renderer.
- Added a composite advanced-rigging acceptance regression across persistence,
  history, timeline synchronization, production preview, and export sampling.
- Added bounded live viewport telemetry for startup, frame percentiles, WebGL
  calls/resources, scene/chunk/effect complexity, and optional browser heap.
- Defined immutable Minimum/Recommended device and Draft/High/Final workload
  budgets with deterministic advisory and hard-limit evaluation.
- Clarified semantic renderer layer ownership and excluded editor-only helpers
  from final preview/export canvases.
- Fixed renderer/material/skin/OBJ/helper geometry cache disposal, audio and
  Blob URL lifecycles, and extracted audio playback ownership from `App.tsx`.
- Added measurable fail-open layer, distance, frustum, and per-chunk culling
  with editor-selection and final-camera correctness.
- Benchmarked global versus chunk-local material batches and shared one owned
  cube geometry across every imported chunk mesh.
- Added bounded renderer-owned VFX geometry, material, and particle-buffer
  pools with deterministic frame reuse and complete overflow/shutdown disposal.
- Made Minecraft material/texture, skin, and parsed OBJ caches renderer-owned;
  OBJ templates now load lazily, clone safely, and prune with visible consumers.
- Fixed replacement/delete/clear disposal in the dormant chunk mesh cache.
- Added a reusable Minecraft chunk-decoding worker with transferable payloads,
  deterministic main-thread fallback, and a closed worker-workload audit.
- Added abort-aware world scans/imports with monotonic operation IDs, active
  worker termination, stale progress/result suppression, and project-replacement
  cancellation; world-import orchestration now lives outside `App.tsx`.
- Deferred ten closed panels plus Blockbench/resource-pack import and production
  render/export workflows until invocation, with localized Suspense/error
  fallbacks and measured main-bundle reduction.
- Added architecture source-size validation, GitHub CI, strict VFX package
  SemVer precedence, and a reproducible manual smoke checklist.

## 0.8.2

- Last explicitly versioned pre-1.0 application baseline. Later phase work
  remains unreleased until an explicit version checkpoint.
