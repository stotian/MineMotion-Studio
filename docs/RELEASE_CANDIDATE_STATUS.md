# Release candidate status

Current state: **source-level beta candidate preparation only**.

No candidate tag has been created because clean dependency installation, complete automated tests/build/audit, native artifacts, installer smoke tests, signing/notarization, visual QA, and remote publication are not all green. Candidate tags are created only after those gates pass.
