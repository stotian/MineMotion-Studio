# Phase 17.8 - Safe Examples, Documentation, and Graph Research

## Scope

- Two deterministic CC0 example packages generated from checked-in declarative
  authoring sources through the production writer and reader.
- Exact archive and SHA-256 synchronization verification through
  `npm run generate:vfx-examples`.
- Author documentation for stack editing, format/security limits, assets,
  permissions, dependencies, installation lifecycle, project embedding, and
  safe failures.
- Evidence-based node-graph decision with explicit future entry criteria.

## Architecture result

The ordered authoring stack remains the sole custom authoring authority. No
second graph document, effect collection, evaluator, or executable package path
was introduced. A future graph must first prove a bounded versioned schema and
compile into the current Primitive V1/runtime path.

## Validation

Final Phase 17 validation is green: deterministic example drift 1/1, focused
8 files/33 tests, full 84 files/384 tests, typecheck, 1,814-module build, and
audit. Generated archives are product examples, not build output, and contain
only root manifest and effect JSON entries.
