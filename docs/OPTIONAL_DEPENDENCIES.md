# Optional dependencies

FFmpeg is optional and is not bundled by default. Users may select a legally obtained executable; MineMotion validates the path, version, codecs, arguments, cancellation, and output directory before use.

Tauri/WebView runtime prerequisites follow the platform package. Resource packs, worlds, audio, models, and extensions remain user-supplied and are never downloaded silently.
